<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical"
    tools:context=".ResultadosBuscaActivity">

    <TextView
        android:id="@+id/txtCabecalhoResultados"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:textColor="@color/primary"
        android:textSize="18sp"
        android:textStyle="bold"
        tools:text="5 lugar(es) encontrado(s)" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/listaResultados"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="8dp" />

    <TextView
        android:id="@+id/txtResultadosVazios"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="24dp"
        android:text="Nenhum lugar encontrado nesta categoria."
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/progressoResultados"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

</LinearLayout>
